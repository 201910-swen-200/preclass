{
  "name": "swen200_preclass_package",
  "version": "1.0.0",
  "description": "Behavior Driven Development.",
  "author": "Steve Spicklemire",
  "devDependencies": {
    "jest-cucumber": "^1.0.9",
    "ts-node": "^7.0.1"
  },
  "scripts": {
    "test": "tsc --noEmit -p . && jest --no-cache",
    "lint": "tsc --noEmit -p . && tslint \"*.ts?(x)\""
  },
  "dependencies": {
    "@types/jest": "^21.1.5",
    "@types/node": "^8.0.47",
    "jest": "^21.2.1",
    "npm": "^6.4.0",
    "ts-jest": "^21.1.3",
    "tslint": "^5.8.0",
    "typescript": "^2.5.3"
  },
  "jest": {
    "transform": {
      ".(ts|tsx)": "ts-jest"
    },
    "testMatch": ["**/*.steps.ts"],
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ]
  }
}
